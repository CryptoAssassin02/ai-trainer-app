import React from 'react'
import '@testing-library/jest-dom'

// The most minimal test possible for Select component
describe('Select Component', () => {
  it('passes a simple test', () => {
    expect(true).toBe(true);
  });
}) 