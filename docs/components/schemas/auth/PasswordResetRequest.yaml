type: object
required: [email]
properties:
  email:
    type: string
    format: email
    description: Email address to send reset link to
    example: user@example.com
example:
  email: user@example.com 