name: sortBy
in: query
description: Field to sort results by
schema:
  type: string
  enum:
    - createdAt
    - updatedAt
    - name
    - date
    - id
    - priority
    - status
  default: createdAt
  example: createdAt
required: false
style: form
explode: false 