description: Database error occurred while processing workout log
content:
  application/json:
    schema:
      $ref: '../../schemas/workoutLogs/WorkoutLogDatabaseError.yaml'
    example:
      status: "error"
      message: "Database error occurred while processing workout log"
      error_type: "constraint_violation"
      details: "Foreign key constraint violation: plan_id does not exist"
      timestamp: "2025-01-15T18:30:00Z" 