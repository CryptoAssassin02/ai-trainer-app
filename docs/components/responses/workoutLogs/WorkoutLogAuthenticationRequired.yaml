description: Authentication required to access workout logs
content:
  application/json:
    schema:
      $ref: '../../schemas/workoutLogs/WorkoutLogAuthenticationError.yaml'
    example:
      status: "error"
      message: "Authentication required to access workout logs"
      error_type: "missing_token"
      timestamp: "2025-01-15T18:30:00Z" 