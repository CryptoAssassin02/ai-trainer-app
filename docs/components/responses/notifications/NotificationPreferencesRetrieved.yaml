description: "User's notification preferences retrieved successfully."
content:
  application/json:
    schema:
      $ref: '../../schemas/notifications/NotificationPreferencesResponse.yaml'
    examples:
      success:
        value:
          status: "success"
          data:
            email_enabled: true
            sms_enabled: false
            push_enabled: true
            in_app_enabled: true
            quiet_hours_start: "22:00"
            quiet_hours_end: "08:00"
            workout_reminders: true
            progress_updates: true
            goal_achievements: false
          message: "Notification preferences retrieved successfully." 