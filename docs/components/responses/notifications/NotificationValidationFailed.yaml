description: "Bad Request - The notification preferences data is invalid."
content:
  application/json:
    schema:
      $ref: '../../schemas/notifications/NotificationValidationError.yaml'
    examples:
      invalidTimeFormat:
        value:
          status: "error"
          message: "Invalid notification preferences provided."
          details:
            - "'quiet_hours_start' must be in HH:MM format (e.g., 22:00)" 