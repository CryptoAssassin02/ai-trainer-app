post:
  tags:
    - Authentication & User Management
  summary: Logout user
  description: |
    Invalidate the current user session and refresh token on the server side.
    Client should also clear stored tokens.
  operationId: logout
  responses:
    '200':
      $ref: '../../components/responses/common/GenericSuccess.yaml'
    '401':
      $ref: '../../components/responses/auth/Unauthorized.yaml'
    '500':
      $ref: '../../components/responses/common/InternalServerError.yaml' 