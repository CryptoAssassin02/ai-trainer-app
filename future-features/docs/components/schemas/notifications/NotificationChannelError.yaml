description: >
  Defines the error response for requests that specify an invalid notification channel.
type: object
required:
  - status
  - message
properties:
  status:
    type: string
    enum: [error]
  message:
    type: string
    example: "Invalid notification channel specified. Must be one of: email, sms, push, in_app." 