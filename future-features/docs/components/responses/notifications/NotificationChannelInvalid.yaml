description: "Bad Request - The specified notification channel is invalid."
content:
  application/json:
    schema:
      $ref: '../../schemas/notifications/NotificationChannelError.yaml'
    examples:
      invalidChannel:
        value:
          status: "error"
          message: "Invalid notification channel specified. Must be one of: email, sms, push, in_app." 