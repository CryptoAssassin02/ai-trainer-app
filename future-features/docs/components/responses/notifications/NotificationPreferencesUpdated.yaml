description: "User's notification preferences updated successfully."
content:
  application/json:
    schema:
      $ref: '../../schemas/notifications/NotificationPreferencesResponse.yaml'
    examples:
      success:
        value:
          status: "success"
          data:
            email_enabled: true
            sms_enabled: true
            push_enabled: true
            in_app_enabled: true
            quiet_hours_start: null
            quiet_hours_end: null
            workout_reminders: true
            progress_updates: false
            goal_achievements: true
          message: "Notification preferences updated successfully." 