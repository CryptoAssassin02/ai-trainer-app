description: "Test notification sent (logged) successfully."
content:
  application/json:
    schema:
      $ref: '../../schemas/notifications/TestNotificationResponse.yaml'
    examples:
      success:
        value:
          status: "success"
          data:
            success: true
          message: "Test email notification logged successfully. Note: Actual delivery depends on user preferences and service configuration." 